1. IIH不能满足需求，特别是 50ms，并且数据模型不具备语义 -> Real Time Industry Collector 40ms 
2. 写代码 -> LCL / UCL（简单配置） -> By SKU（高级配置） -> Attribute（语义配置）-> Ready For AI
3. 后续管理 -> Excel 导入 -> (后续 AI 生成)语义数据模型 -> 自动适配 Collector 和 UI Board

汇报思路：
1. 好的，谢谢 mingxi，非常荣幸能够代表团队来分享与黄埔的专家们共创 board2 的一些经验。我也很开心能够来到黄埔工厂，黄埔工厂可以说是我见过数字化程度最高的工厂了。
2. 说到 board2，其实在与MingXi初次沟通时，我们内部以为就是一个普通的数采及看板项目，但随着沟通的深入，我们发现黄埔的专家们有一个更大的愿景，从智能采集到智能展示再到智能分析，最后是智能控制，这是一个完整的闭环，我个人非常欣赏这个愿景，也非常开心能够参与到这个项目中。
3. 基于这样一个长远的目标，我们在 Board2 的架构上做了很大的调整。最大的调整就是构建了一个位于边缘的语义数据模型。什么叫语义数据模型呢？我们可以想象在 plc 中的数据，它的数据就是 tag 与 value，而我们这里可以看到，不单单能够表达设备的从属关系，还能把业务专家的关心的业务逻辑也描述进去。比如这里，业务专家可以使用自然语言来描述需要在产线级别统计一个班次总的停机时间，并且描述这个统计信息与哪些数据关联，我们在构建 UI 时，就能够自动识别该规则，自动进行计算，不需要业务专家再去构建看板了。当然，现在大家看到的看板还是一个固定的看板，未来，我们是可以基于 AI 去分析语义数据模型，来根据黄埔专家的需要，来动态生成看板，我觉得语义数据模型是实现智能化的快速，也是基础。
4. 